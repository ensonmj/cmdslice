<% if res && res.errors.any? %>
  <div class="err_msg">
    <h4 class="alert alert-danger"><%= pluralize(res.errors.count, "error") %> occured:</h4>
    <ul>
      <% res.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
