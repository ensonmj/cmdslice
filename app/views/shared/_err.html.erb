<% if res && res.errors.any? %>
  <div class="err_msg">
    <h3 class="alert alert-danger"><%= pluralize(res.errors.count, "error") %> occured:</h3>
    <ul>
      <% res.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
