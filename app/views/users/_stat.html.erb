<div class="panel panel-default stat">
  <div class="panel-heading">
  <h4 class="panel-title title">User Stat</h4>
</div>
  <div class="panel-body">
    <p>You have <%= pluralize(current_user.slices.count, "slice") %>,
      <%= pluralize(current_user_got_comments, "comment") %>.</p>
    <p>Yield <%= pluralize(current_user.comments.count, "comment") %>.</p>
  </div>
</div>
