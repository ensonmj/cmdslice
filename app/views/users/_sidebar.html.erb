<% if current_user == @user %>
  <div class="user-btn">
    <div class="btn-group">
      <%= link_to "New slice", new_slice_path, class: "btn btn-success" %>
      <%= link_to "New Ask", new_user_ask_path(@user), class: "btn btn-success" %>
    </div>
  </div>
<% end %>
<%= render 'stat' %>
<%= render 'shared/site_stat' %>
